#include <stdio.h>
//To check whether number is positive negative or equal to zero
int main(){
  int num;
  printf("\nenter any number");
  scanf("%d",&num);
  if(num==0)
    printf("\nThe value is equal to zero");
  if else(num>0)
    printf("\nThe value is positive");
    else(num<0)
    printf("\nThe value is negative);
      return 0;
}
